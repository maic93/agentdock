# scripts/ci/

Reserved for CI helper scripts that grow too complex to inline in
`.github/workflows/ci.yml`. Currently empty — the CI workflow's steps are
still simple enough to live directly in the YAML; extracting them here
prematurely would add indirection without benefit.
