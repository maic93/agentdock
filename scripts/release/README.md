# scripts/release/

Reserved for release automation beyond what `pnpm changeset publish`
(see `.changeset/README.md`) handles directly. Currently empty — no
packages are published yet, so there is nothing to automate beyond the
Changesets workflow itself.
