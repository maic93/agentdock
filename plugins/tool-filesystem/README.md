# @agentdock/plugin-tool-filesystem

Reference plugin. Not implemented yet — depends on `@agentdock/shared-sdk`
and `@agentdock/provider-abstraction`, which must exist first. See
`plugins/README.md` for what this directory is for and its boundaries.
