# Plugin template

Reserved location for a minimal, copyable plugin starter (manifest +
lifecycle hook stubs) once `@agentdock/shared-sdk` exists to scaffold
against. Not populated yet — see `templates/README.md` and
`docs/plugin-guide/README.md`.
