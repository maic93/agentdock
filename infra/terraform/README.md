# infra/terraform

Optional reference infrastructure-as-code for a hosted/cloud deployment of
AgentDock, should a hosted offering (see the Phase 4 roadmap item in
`docs/architecture/001-system-architecture.md`, Section 24) be pursued.

## Status

Deferred, same reasoning as `infra/helm/README.md` — this is scoped for a
post-core-implementation phase, not the current repository foundation.
